# ORACLE EXTERNAL THEORY FEEDER — 24 SHARP PROMPTS

Every response is an untrusted seed. Require auditable sources for literature claims. Reject any answer that conflates arbitrary tuples with one-parameter polynomial progressions.

## 1. C3 breakpoint mechanism

Given the exact arbitrary-tuple equation x1−3x2+3x3−x4=0 and the attached semantics, propose a necessary mechanism explaining the certified 11 plateau and first 12-set. Do not equate this with a one-parameter polynomial progression. Return assumptions, one falsifiable lemma, deterministic operators, and least-counterexample probes.

## 2. C3 gap inequality

Derive a candidate upper-bound inequality in consecutive gaps that would imply |A|≤11 on the target strip. State every quantifier, a deterministic weight-search procedure, and a symbolic checker.

## 3. C3 birth motif

Given complete 11- and 12-extremizer families, propose an invariant that distinguishes every first 12-set from every plateau extremizer. Return necessity, sufficiency, and falsification tests.

## 4. C6 plateau classification

Propose a structural proof strategy for C6(N)=8 on 9≤N≤15 under arbitrary-tuple semantics. Prefer a complete grammar, extension obstruction, or local occupancy theorem over enumeration.

## 5. Uniform Pascal obstruction

Use Pascal identities carefully to propose a k-parameter obstruction covering C4, C5, and C6. Explicitly list all intermediate-membership assumptions and separating counterexamples.

## 6. Cross-order transfer

Propose a deterministic transform between adjacent orders, or explain precisely why natural deletion, difference, and digit transforms fail. Return exact preservation predicates.

## 7. Sidon–C3 repair

Find either a structural sufficient condition making a Sidon set C3-free or a size-preserving exchange operation that removes every C3 violation. Distinguish existential and universal non-binding.

## 8. Sidon-compatible violations

Classify all nontrivial C3 solutions possible inside a Sidon set. Return a canonical parameterization and equality-partition audit.

## 9. C2–C3 synergy

Propose a theorem proving genuine interaction between C2 and C3 rather than dominance by one constraint. Prefer a mixed-core or quantitative violation-coupling statement.

## 10. Coefficient universality

Given exact results for many primitive zero-sum coefficient vectors, propose invariant-defined universality classes, a theorem candidate for one class, and the nearest-counterexample campaign.

## 11. Pascal exceptionalism

Specify a fair complete matched coefficient class and one precise theorem or counterexample test for whether alternating Pascal vectors are exceptional.

## 12. Parametric mutation theorem

Choose a one-parameter primitive zero-sum coefficient family. Propose a piecewise exact theorem, support-change thresholds, and a deterministic coefficient-cell decomposition.

## 13. Extremizer grammar

Given every optimum set, propose the smallest grammar or automaton likely to generate all of them. Specify soundness, completeness, and held-out prediction checks.

## 14. Stability theorem

Propose a quantitative theorem for all sets of size Ck(N)−1 near a plateau. Return a repair map, exact edit-radius target, and a counterexample search.

## 15. Supersaturation

Propose a sharp lower bound on the number of distinct forbidden supports in a set of size Ck(N)+t. Keep canonical supports separate from ordered tuples.

## 16. Carry-free construction

Design a base-B digit construction for a specified coefficient vector. Derive the exact signed carry margin and all proof obligations.

## 17. Finite-field construction

Propose a finite-field graph or polynomial-image construction, then specify the integer embedding and every possible new collision.

## 18. Finite-to-infinite upper bound

Use a certified local obstruction or supersaturation lemma to propose an all-N bound via overlapping windows, containers, or induction with explicit boundary correction.

## 19. Clause-to-lemma compression

Given SAT/ILP proof artifacts, propose how to quotient clauses into a small quantified lemma basis. Return equivalence checks and a transparent proof-length metric.

## 20. Breakpoint predictor

From a frozen exact prefix, propose a preregisterable symbolic breakpoint law and the unseen range that would decisively test it.

## 21. Applicable literature theorem

Identify primary-source theorems that may apply to this exact arbitrary-tuple invariant equation. For each provide URL/DOI, theorem location, hypotheses, implication chain, and failed-hypothesis counterexamples.

## 22. Novelty collision audit

Search exact coefficient vectors, normalized variants, sequence prefixes, plateaus, and intersections under alternative terminology. Return auditable source metadata; do not conclude novelty from search silence alone.

## 23. Transfer blade specification

Take one successful theory mechanism and define its trigger predicate, immutable operator DAG, deterministic parameter rules, negative boundary, and sealed second-target selector.

## 24. Hostile referee

Attack a proposed theorem on tuple semantics, degeneracy, certificate independence, arbitrary-tuple/progression conflation, quantifiers, prior work, proof compression, and transfer cheating. Separate fatal, major, and minor issues.
